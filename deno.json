{
  "imports": {
    "@std/http": "jsr:@std/http"
  },
  "tasks": {
    "dev": "deno run --allow-net --allow-read serve.ts",
    "build": "deno bundle --outdir dist index.html --minify && cp -r assets favicon.ico dist/",
    "prod": "deno run --allow-net --allow-read serve.ts -- --prod"
  }
}
